\documentclass[../main.tex]{subfiles}
\begin{document}

% Энэ бүлэг үнэлгээний гол шалгуурт (асуудал дэвшүүлэн шийдэл гаргасан эсэх — 15 оноо)
% чиглэнэ: тулгарсан бодит асуудлууд ба шийдлүүд.

\section{Туршилтын арга зүй}

% TODO: smoke-test.sh (бүртгэл -> 401 шалгалт -> paste -> ready -> chat -> sources),
% typecheck/build, гар туршилт (вэб + бодит утас)

\section{Асуудал 1: Хэл дамнасан хайлтын чанар}

% TODO: Латин асуулт vs кирилл баримт -> ~0.35 косинус, 0.7 босго доор (ERR-021);
% шийдэл: босго 0.1, lambda 0.5, системийн зааварт эх баримтын хэлээр хариулах;
% ирээдүйн шийдэл: асуултын орчуулга (query translation)

\section{Асуудал 2: Том хэмжээний текст оруулах}

% TODO: 50k тэмдэгт -> 429 RESOURCE_EXHAUSTED (ERR-027); 500k -> 413 bodyLimit;
% шийдэл: токен төсөвт багцууд, 3с завсарлага, exponential backoff;
% үр дүн: 50k -> 95с (5 удаагийн 429-ийг даван), 500k -> 492 chunk / 41 багц

\section{Асуудал 3: Мобайл төхөөрөмжийн boot loop}

% TODO: эмуляторын хаяг бодит утсан дээр ажиллахгүй -> onboarding давталт;
% шийдэл: зэрэгцээ liveness probe, 5с timeout, зөв fallback route

\section{Гүйцэтгэлийн хэмжилтүүд}

% TODO: хүснэгтээр — ingestion хугацаа (баримтын хэмжээгээр), хариултын хугацаа,
% chunk/batch тоо, туршилтын үр дүн

\end{document}
