\documentclass[../main.tex]{subfiles}
\begin{document}

\section{Даалгаврын тодорхойлолт}

% TODO: Асуудлын томьёолол — байгууллагын баримт бичгүүдээс мэдээллийг хайж олох
% өнөөгийн процессын бэрхшээл, түүнийг RAG chatbot-оор шийдэх зорилт.
% Даалгавар: PDF болон текст баримтууд дээр суурилан асуултад хариулдаг,
% эх сурвалжаа ишэлдэг, монгол хэлийг дэмждэг чатбот систем бүтээх.

\section{RAG буюу хайлтаар өргөтгөсөн хариу үүсгэлт}

% TODO: RAG-ийн үндсэн ойлголт \cite{lewis2020rag} — LLM-ийн мэдлэгийн хязгаарлалт,
% hallucination асуудал, түүнийг гадаад мэдлэгийн сангаар шийдэх зарчим.

\section{Embedding ба вектор хайлт}

% TODO: текстийг вектор огторгуйд буулгах, косинус төстэй байдал,
% pgvector \cite{pgvector}, HNSW индекс \cite{malkov2018hnsw}

\section{MMR буюу олон талт байдлын дахин эрэмбэлэлт}

% TODO: MMR алгоритм \cite{carbonell1998mmr} — хамаарал ба олон талт байдлын
% тэнцвэр, $\lambda$ параметр

\section{Том хэлний загварууд ба Gemini API}

% TODO: Gemini generation + embedding загварууд \cite{geminiapi},
% үнэгүй түвшний хязгаарлалтууд, загварын сонголтын үндэслэл

\end{document}
